syntax = "proto3";

option java_package = "com.example.weatherapp.data";
option java_multiple_files = true;

message UserPreferences {
  bool is_favourites_modified = 1; // specifies if the user has edited the favourites screen
}